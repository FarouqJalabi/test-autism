<h1 class="font-bold  text-3xl text-center">Our blogs</h1>
<div class="flex flex-wrap items-center justify-center gap-32 p-5">
  <%= render @blog_posts %>
  <%= render @blog_posts %>
  <%= render @blog_posts %>
  <%= render @blog_posts %>
  <%= render @blog_posts %>
</div>

<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Gain insights into your traits. This test is not a diagnostic tool but can help you understand yourself better.">
    <title>Questioner</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "quizPage", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav>
        <a href="/" class="logo" target="_blank">
          <%= image_tag "Logo.svg", alt: "logo" %>
        </a>

      <%= link_to "Home", root_path, class: ('selected_nav_element' if on_home?) %>
      <%= link_to "Take a test", test_path(1), class: ('selected_nav_element' if on_test?) %>
      <%= link_to "About", root_path+"#aboutTest", class: ('selected_nav_element' if on_about?) %>
      
    </nav>

    <%= yield %>
  </body>
</html>
<style>
  .dropDown {
      background: #bfbfbf;
      padding: 5px;
      position: relative;
  }
  .dropDown ul {
      /*display: none;*/
      list-style-type: none;

      position: absolute;
      left: 0;

      background-color: inherit;
      width: 100%;
  }
  .dropDown ul li {
     padding: 5px;
  }

  .dropDown button {
      background-color: transparent;
      border: none;
      font-size: large;
  }

</style>

<select id="locale-select" name="" onchange="window.location.href=this.value">
  <option <%=  "selected" if I18n.locale == :en %> value="<%= url_for(params.permit(:score).merge(locale: :en)) %>"><span>🇺🇸</span>English</option>
  <option <%=  "selected" if I18n.locale == :es %> value="<%= url_for(params.permit(:score).merge(locale: :es)) %>"><span>🇪🇸</span>Español</option>
  <option <%=  "selected" if I18n.locale == :fr %> value="<%= url_for(params.permit(:score).merge(locale: :fr)) %>"><span>🇫🇷</span>Français</option>
  <option <%=  "selected" if I18n.locale == :nb %> value="<%= url_for(params.permit(:score).merge(locale: :nb)) %>"><span>🇳🇴</span>Norsk</option>
</select>
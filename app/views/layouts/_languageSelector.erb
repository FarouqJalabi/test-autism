<style>
  .dropDown {
      background: #bfbfbf;
      padding: 5px;
      position: relative;
  }
  .dropDown ul {
      /*display: none;*/
      list-style-type: none;

      position: absolute;
      left: 0;

      background-color: inherit;
      width: 100%;
  }
  .dropDown ul li {
     padding: 5px;
  }

  .dropDown button {
      background-color: transparent;
      border: none;
      font-size: large;
  }

</style>
<%= link_to("helo", { locale: locale }, class: "language-link") %>
<select id="locale-select" name="" id="" onchange="window.location.href=this.value">

  <option value="<% url_for({locale: :nb}) %>">Change Language</option>
  <option value="<%= url_for({locale: :en}) %>"><span>🇺🇸</span>English</option>
  <option value="<%= url_for({locale: :es}) %>"><span>🇪🇸</span>Español</option>
  <option value="<%= url_for({locale: :fr}) %>"><span>🇫🇷</span>Français</option>
</select>
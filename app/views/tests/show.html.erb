<main class="">
  <progress class="progress progress-primary w-full" value="0" max="100"></progress>

  <%= form_with url: questions_calculate_answer_path, method: :post do |form| %>
    <div class="relative h-80 w-96 m-auto">
      <%= render @questions, form: form %>
    </div>
  <% end %>
</main>

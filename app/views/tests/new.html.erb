<% content_for :title, "New test" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New test</h1>

  <%= turbo_frame_tag dom_id(@test) do %>
    <%= render "form", test: @test %>
  <% end %>

  <%= link_to "Back to tests", tests_path, class: "ml-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium" %>
</div>
